{
  "scripts": {
    "wp:up": "docker compose up -d && google-chrome --profile-directory='Default' http://localhost:8080/",
    "wp:down": "docker compose down",
    "ssg": "./ssg.sh",
    "preview": "npx serve dist"
  },
  "dependencies": {
    "serve": "^14.2.5"
  }
}
